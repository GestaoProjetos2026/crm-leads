import { Injectable } from '@nestjs/common';

/**
 * AuthService — stub for Step 01.
 * Full implementation (login, token issuance, JWT strategy) in Step 02.
 */
@Injectable()
export class AuthService {}
